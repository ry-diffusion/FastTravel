# Developer API key issued by vrSrc for the VR CyberDeck application.
# Required at build time — copy this file to .env and fill in the value.
# The .env file is gitignored and must never be committed.
# In CI/CD, set VRSRC_API_KEY as a repository secret instead.
VRSRC_API_KEY=
